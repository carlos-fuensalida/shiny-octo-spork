# Feature Specs

One flat file per Jira ticket: `specs/features/SPM-<n>-<slug>.md` — no per-ticket subfolder. Created when a ticket is scoped (see `CLAUDE.md` → Planning & Jira Workflow), holding whatever spec content that ticket needs.
